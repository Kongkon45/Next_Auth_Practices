/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : ["www.themealdb.com", "picsum.photos", "i.ibb.co.com", "lh3.googleusercontent.com", "avatars.githubusercontent.com"]
    }
};

export default nextConfig;
